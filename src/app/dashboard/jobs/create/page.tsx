import { CreateJobTemplate } from "@/templates/Dashboard/Jobs/Create/CreateJobTemplate";

export const metadata = {
	title: "Create Job | Dashboard"
};

export default function CreateJobPage() {
	return <CreateJobTemplate />;
}
